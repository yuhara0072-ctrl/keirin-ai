# コピーして secrets.toml として使用（git には含めない）
# Streamlit Cloud ではアプリ設定 → Secrets に同じ内容を貼り付け

[auth]
username = "your_username"
password = "your_password"

# 任意: Cookie 署名専用（未設定時は password から自動導出）
# cookie_secret = "your_random_secret"
